<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.flab.marketflea.mapper.UserMapper">

    <insert id="add" parameterType="com.flab.marketflea.dto.user.User">
        INSERT
        INTO user(id, password, name, role, phone, email, address, createdAt, updatedAt)
        VALUES (#{id}, #{password}, #{name}, #{role}, #{phone}, #{email}, #{address}, now(), now())
    </insert>

    <select id="isIdExist" resultType="_boolean">
        SELECT EXISTS(SELECT * FROM user where id = #{id})
    </select>

</mapper>